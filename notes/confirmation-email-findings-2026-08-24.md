# Confirmation-email findings

2026-08-28 verification-code update: Auth.tsx now shows a six-digit code field after signup when Supabase returns no session, verifies with `supabase.auth.verifyOtp({ type: "signup" })`, supports resend, and removes the forgotten-password UI/routes. Added tested signupVerification helpers. Supabase Confirm sign up template subject was set to `ELIZZY DOMAIN` and body uses `{{ .Token }}` with branded code instructions; the template save state reported no pending changes. Supabase SMTP currently shows custom SMTP enabled with sender `no-reply@elizzy-host.nx.kg` and sender name `ELIZZY DOMAIN`. The requested `mikeakex80@gmail.com` sender cannot be assumed valid through the current Resend setup without provider verification or switching to Gmail SMTP with an app password; no credential was stored in this note.

Gmail SMTP research: Supabase’s official guidance states the sender email and SMTP username must be the Google Workspace/admin email, the SMTP password must be a Google App Password after enabling 2FA, and smtp.gmail.com supports ports 465 or 587. This means mikeakex80@gmail.com can be used only with Gmail SMTP and an App Password, not as an unverified Resend sender. Source: https://supabase.com/docs/guides/troubleshooting/using-google-smtp-with-supabase-custom-smtp-ZZzU4Y

2026-08-28 Gmail SMTP cutover: Supabase SMTP now shows custom SMTP enabled with sender `mikeakex80@gmail.com`, sender name `ELIZZY DOMAIN`, host `smtp.gmail.com`, port `465`, username `mikeakex80@gmail.com`, and a populated masked password field after the user saved the App Password. Supabase displayed its standard warning that Gmail is personal rather than transactional; no credential was recorded.

2026-08-28 template repair: the Supabase Confirm sign up template was corrected through the live Monaco editor after an earlier coordinate edit had appended malformed content. The saved state now shows subject `ELIZZY DOMAIN` and one clean HTML body containing `{{ .Token }}` and six-digit verification instructions; no credential was recorded.

2026-08-28 final template verification: after reloading and using the actual Monaco editor instance, the Supabase Confirm sign up template was saved cleanly. Current visible state is subject `ELIZZY DOMAIN` and one valid body with `{{ .Token }}` and six-digit activation instructions; the earlier duplicated/malformed unsaved editor content is no longer present.

2026-08-28 re-verification: Gmail SMTP had previously rendered with custom SMTP enabled, sender `mikeakex80@gmail.com`, host `smtp.gmail.com`, port `465`, and a masked saved App Password. A later Supabase navigation showed the session-expired dialog before the fields could be re-read; no credential was exposed or recorded.

2026-08-28 live UI verification: `https://streamvideo-h2f3nxnx.manus.space/?mode=signup` renders the ELIZZY DOMAIN signup form with the text “After signup, enter the six-digit code sent from ELIZZY DOMAIN to activate your account.” The live login route renders normal email/password sign-in and does not show a Forgot password option. No real signup was submitted because a unique recipient address is required for a safe delivery test.

2026-08-28 delivery test: signup for `eliminatortech22@gmail.com` returned HTTP 200 from Supabase `/auth/v1/signup` with `confirmation_sent_at`, and the app displayed the six-digit verification screen. The app-level `Resend code` action also completed and displayed “A new six-digit verification code was sent. Check your inbox and spam folder.” The network log showed no client-visible SMTP error. The same log also recorded a non-blocking 401 profile insert because the signup profile RLS policy does not permit the anonymous client to insert; this should be reviewed separately from email delivery.

2026-08-28 diagnosis: Supabase `/auth/v1/signup` returned HTTP 200 with `confirmation_sent_at` for eliminatortech22@gmail.com, and `/auth/v1/resend` returned HTTP 200 with an empty JSON body. This confirms the client reached Supabase and Supabase accepted both send requests, but it does not prove Gmail SMTP delivered the messages. The client also attempted an anonymous `profiles` upsert and received HTTP 401 from RLS; that write was removed, while the authenticated post-verification profile upsert remains. Added `authProfile` regression coverage.

2026-08-28 SMTP persistence check: after the user entered the Gmail values and the page was saved, a fresh authenticated reload again showed the old Resend sender `no-reply@elizzy-host.nx.kg`, host `smtp.resend.com`, and username `resend`. Therefore the Gmail configuration still has not persisted in Supabase; the missing email is not an application-side send failure. No password value was recorded.

2026-08-28 latest SMTP attempt: the authenticated page currently shows sender and username `mikeakex80@gmail.com`, host `smtp.gmail.com`, port `465`, sender name `ELIZZY DOMAIN`, and custom SMTP enabled. The previous attempt had the username incorrectly set to the sender name; it was corrected before saving. A fresh reload is still required to confirm persistence.

2026-08-28 final persistence check: after the latest attempted save, a fresh Supabase reload again showed the old Resend values (`no-reply@elizzy-host.nx.kg`, `smtp.resend.com`, username `resend`). The browser console showed no client-side error. The SMTP form therefore appears to be rejecting or not committing the Gmail change on the Supabase side; the app cannot fix this through frontend code.

2026-08-28 immediate-access change: Auth.tsx no longer renders verification-code or resend controls, signup no longer attempts an anonymous profiles upsert, and legacy confirmation-redirect/password-reset/signup-verification modules were removed. Signup now reports immediate account access when Supabase returns a session; normal email/password login remains. Validation passed with 25 test files / 93 tests and a production build. Supabase’s separate Confirm email setting still must be disabled in the dashboard for signup to return a session.

2026-08-28 immediate-access policy check: Supabase Sign In / Providers was opened after the user’s takeover, but the User Signups panel remained in a loading state and did not expose the Confirm email control. A follow-up view still showed only skeleton rows, so the dashboard policy change cannot yet be verified.

2026-08-28 email-policy verification: the preview signup form rejected `policy-test@mailinator.com` before any Supabase request and displayed “Temporary or disposable email addresses are not accepted.” The mobile signup screen no longer shows confirmation-code or resend controls. Supabase’s User Signups page still requires a separate authenticated dashboard change to disable Confirm email; its session expired during the last inspection.

2026-08-28 provider settings handoff: Supabase Sign In / Providers is open for the user. The User Signups panel is still loading in the browser, so the Confirm email toggle is not yet interactive; user takeover/sign-in is required before it can be disabled.

2026-08-28 confirmation policy verification: Supabase User Signups is authenticated and rendered. DOM switch inspection confirms Allow new users to sign up is enabled and Confirm email is disabled (`aria-checked=false`, `data-state=unchecked`). This satisfies the immediate-access policy at the Supabase project level.
